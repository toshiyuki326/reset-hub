import {existsSync,readFileSync} from 'node:fs';
const envPath=process.argv[2]||'.env.staging.local';if(!existsSync(envPath)){console.error(`FAIL ${envPath} is missing. Copy .env.staging.example and add staging public values.`);process.exit(1)}const env=Object.fromEntries(readFileSync(envPath,'utf8').split(/\r?\n/).filter(x=>x&&!x.startsWith('#')).map(x=>{const i=x.indexOf('=');return [x.slice(0,i),x.slice(i+1)]}));const errors=[];if(!/^https:\/\/[a-z0-9-]+\.supabase\.co$/.test(env.VITE_SUPABASE_URL||''))errors.push('VITE_SUPABASE_URL must be a staging Supabase URL');if(!env.VITE_SUPABASE_ANON_KEY)errors.push('VITE_SUPABASE_ANON_KEY is missing');if(env.VITE_USE_FIXTURES!=='false')errors.push('VITE_USE_FIXTURES must be false');for(const secret of ['SUPABASE_SERVICE_ROLE_KEY','LINE_CHANNEL_SECRET','LINE_CHANNEL_ACCESS_TOKEN','GOOGLE_CLIENT_SECRET','GOOGLE_TOKEN_ENCRYPTION_KEY'])if(env[secret])errors.push(`${secret} must not be in the frontend staging env file`);if(errors.length){errors.forEach(x=>console.error(`FAIL ${x}`));process.exit(1)}console.log('PASS staging frontend environment is explicit, non-fixture, and contains no server-only secret names.');
