import {readdirSync,readFileSync,statSync} from 'node:fs';import {join} from 'node:path';const root='dist';if(!statSync(root,{throwIfNoEntry:false}))throw new Error('dist is missing; run npm run build first');const files=[];const walk=p=>readdirSync(p).forEach(n=>{const x=join(p,n);if(statSync(x).isDirectory())walk(x);else files.push(x)});walk(root);const text=files.filter(x=>/\.(js|css|html|json|webmanifest)$/.test(x)).map(x=>readFileSync(x,'utf8')).join('\n');const forbidden=['SUPABASE_SERVICE_ROLE_KEY','LINE_CHANNEL_SECRET','LINE_CHANNEL_ACCESS_TOKEN','GOOGLE_CLIENT_SECRET','GOOGLE_TOKEN_ENCRYPTION_KEY','OPENAI_API_KEY','encrypted_refresh_token'];const found=forbidden.filter(x=>text.includes(x));if(found.length){console.error(`FAIL server-only identifiers found in production build: ${found.join(', ')}`);process.exit(1)}console.log(`PASS inspected ${files.length} build files; no server-only secret identifiers found.`);
