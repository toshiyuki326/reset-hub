import {useState} from 'react';import {Navigate} from 'react-router-dom';import {Button} from '../components/ui';import {useAuth} from '../app/AuthProvider';import {toast} from 'sonner';
export default function LoginPage(){const auth=useAuth();const [sent,setSent]=useState(false);const [email,setEmail]=useState('');const [busy,setBusy]=useState(false);if(auth.session)return <Navigate to="/" replace/>;const submit=async()=>{setBusy(true);try{await auth.signIn(email);setSent(true)}catch{toast.error('ログインリンクを送信できませんでした。')}finally{setBusy(false)}};return <main className="login"><div className="login-brand"><span>r/</span><b>reset HUB</b><p>運営の「今」を、ひとつに。</p></div><section><p>WELCOME BACK</p><h1>ログイン</h1><span>招待されたメールアドレスを入力してください。ログイン用リンクをお送りします。</span>{sent?<div className="sent">メールを送信しました。受信トレイをご確認ください。</div>:<><label>メールアドレス<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><Button disabled={busy||!email} onClick={submit}>{busy?'送信中…':'Magic Linkを送信'}</Button></>}</section></main>}
