import {Plus} from 'lucide-react';import {useStore} from '../app/Store';import {Button,Pill} from '../components/ui';
export default function MembersPage(){const {members}=useStore();return <div className="page"><header className="page-head"><div><p>TEAM</p><h1>メンバー</h1></div><Button onClick={()=>alert('招待メール送信はSupabase接続後に利用できます')}><Plus/>メンバーを招待</Button></header><div className="members">{members.map(m=><div key={m.id}><i>{m.avatar}</i><span><strong>{m.displayName}</strong><small>{m.email}</small></span><Pill tone={m.role==='owner'?'green':'gray'}>{m.role}</Pill><small>{m.active?'有効':'無効'}</small></div>)}</div></div>}
