import {describe,expect,it} from 'vitest';import {readFileSync} from 'node:fs';
const migration=readFileSync(new URL('../../supabase/migrations/202608130004_project_ai_persistence_foundation.sql',import.meta.url),'utf8');const bootstrap=readFileSync(new URL('../../supabase/migrations/202608130003_fix_owner_bootstrap.sql',import.meta.url),'utf8');
describe('project and AI persistence migration',()=>{for(const table of ['project_goals','project_kpis','project_kpi_entries','ai_conversation_sessions','ai_conversation_messages','ai_usage_events'])it(`creates and protects ${table}`,()=>{expect(migration).toContain(`create table public.${table}`);expect(migration).toContain(`alter table public.${table} enable row level security`)});it('keeps proposal review/approval/execution fields',()=>{expect(migration).toContain("'proposal','review','approved','rejected','executed','failed'");expect(migration).toContain("proposal_status not in ('approved','executed')")});it('enforces community-safe Goal/KPI and Session/Message foreign keys',()=>{expect(migration).toContain('foreign key(goal_id,community_id)');expect(migration).toContain('foreign key(session_id,community_id)')});it('does not regress the corrected Owner bootstrap',()=>expect(bootstrap).toContain('on conflict on constraint community_members_community_id_profile_id_key do nothing'))});
