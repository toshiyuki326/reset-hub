import {describe,expect,it} from 'vitest';import {readFileSync} from 'node:fs';const sql=readFileSync(new URL('../../supabase/migrations/202608120002_production_hardening.sql',import.meta.url),'utf8');
describe('invitation and membership contract',()=>{it('matches JWT email to an active unused invitation',()=>{expect(sql).toContain("lower(coalesce(auth.jwt()->>'email',''))");expect(sql).toContain('accepted_at is null');expect(sql).toContain('expires_at>now()')});it('creates an active membership with the invitation role',()=>expect(sql).toContain('values(v_inv.community_id,v_profile,v_inv.role,true)'))});
