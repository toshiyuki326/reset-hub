import {describe,expect,it} from 'vitest';import {readFileSync} from 'node:fs';import {buildProjectAiInput,projectAiSystemInstruction} from '../../supabase/functions/project-ai-chat/prompt';import {mapProjectAiContext} from '../../supabase/functions/project-ai-chat/context';
const read=(name:string)=>readFileSync(new URL(`../../supabase/functions/project-ai-chat/${name}`,import.meta.url),'utf8');const index=read('index.ts');const schema=read('schema.ts');const provider=read('provider.ts');const frontend=readFileSync(new URL('../services/projectAiService.ts',import.meta.url),'utf8');const main=readFileSync(new URL('../main.tsx',import.meta.url),'utf8');
describe('Project AI Edge Function',()=>{it('requires JWT, active membership and session ownership',()=>{expect(index).toContain('admin.auth.getUser(jwt)');expect(index).toContain(".eq('active',true)");expect(index).toContain('profile.id!==session.profile_id');expect(index).not.toContain('community_id:parsed.data')});it('maps only supplied context and explicitly has no project/documents',()=>{const context=mapProjectAiContext({generatedAt:'now',profile:{id:'p',display_name:'Owner'},role:'owner',community:{id:'c',name:'reset',slug:'reset'},tasks:[{id:'t'}],goals:[{id:'g'}],kpis:[{id:'k'}],activity:[{id:'a'}],history:[{id:1},{id:2}]});expect(context.project).toBeNull();expect(context.documents).toEqual([]);expect(context.openTasks).toEqual([{id:'t'}]);expect(context.conversationHistory).toEqual([{id:2},{id:1}])});it('builds a read-only, supplied-context-only prompt',()=>{expect(projectAiSystemInstruction).toContain('読み取り専用');expect(projectAiSystemInstruction).toContain('自律的な実行');expect(projectAiSystemInstruction).toContain('提供されたContextだけ');expect(buildProjectAiInput({documents:[]},'KPIは？')).toContain('KPIは？')});it('validates structured output and rejects unknown proposal kinds',()=>{expect(schema).toContain("z.enum(allowedProposalKinds)");expect(schema).toContain("'create_task','update_task','create_goal','update_goal','create_event'");expect(schema).toContain('.strict()');expect(provider).toContain('aiResponseSchema.safeParse')});it('persists proposal without executing it',()=>{expect(index).toContain("proposal_status:proposal?'proposal':'none'");expect(index).not.toMatch(/from\('(tasks|events|project_goals)'\)\.insert/);expect(frontend).toContain("functions.invoke('project-ai-chat'")});it('records safe usage and classified provider failures',()=>{expect(index).toContain("from('ai_usage_events').insert");expect(index).toContain('input_units:result.inputTokens');expect(index).toContain('output_units:result.outputTokens');expect(index).not.toMatch(/metadata:\{[^}]*prompt/);expect(provider).toContain("'RATE_LIMIT'|'PROVIDER_UNAVAILABLE'|'INVALID_STRUCTURED_RESPONSE'")});it('lazy-loads Project AI and keeps it out of static imports',()=>{expect(main).toContain("lazy(()=>import('./pages/ProjectAiPage'))");expect(main).not.toContain("import ProjectAiPage from './pages/ProjectAiPage'")})});
