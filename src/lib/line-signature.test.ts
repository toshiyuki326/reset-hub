import {describe,expect,it} from 'vitest';import {verifyLineSignature} from '../../supabase/functions/_shared/line-signature';
async function sign(body:string,secret:string){const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const digest=await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(body));return btoa(String.fromCharCode(...new Uint8Array(digest)))}
describe('LINE raw-body signature',()=>{it('accepts a valid HMAC-SHA256 signature',async()=>{const body='{"events":[]}';expect(await verifyLineSignature(body,await sign(body,'staging-test-secret'),'staging-test-secret')).toBe(true)});it('rejects an invalid signature',async()=>expect(await verifyLineSignature('{"events":[]}','invalid','staging-test-secret')).toBe(false))});
