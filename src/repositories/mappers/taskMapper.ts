import type {TaskContract} from '../../types/contracts';
export const mapTaskRow=(r:Record<string,unknown>):TaskContract=>({id:String(r.id),communityId:String(r.community_id),title:String(r.title),description:String(r.description||''),status:r.status as TaskContract['status'],priority:r.priority as TaskContract['priority'],assigneeId:r.assignee_id?String(r.assignee_id):undefined,dueAt:r.due_date?String(r.due_date):undefined,eventId:r.event_id?String(r.event_id):undefined,source:{type:r.source_type as TaskContract['source']['type'],id:r.source_id?String(r.source_id):undefined},createdBy:String(r.created_by),createdAt:String(r.created_at),updatedAt:String(r.updated_at),completedAt:r.completed_at?String(r.completed_at):undefined});
