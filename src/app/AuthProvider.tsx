/* eslint-disable react-refresh/only-export-components */
import {createContext,useContext,useEffect,useState,type ReactNode} from 'react';import type {Session} from '@supabase/supabase-js';import {fixtureMode,supabase} from '../lib/supabase';
type Auth={session:Session|null;loading:boolean;error:boolean;fixture:boolean;signIn:(email:string)=>Promise<void>;signOut:()=>Promise<void>};const C=createContext<Auth|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const [session,setSession]=useState<Session|null>(null);const [loading,setLoading]=useState(!fixtureMode);const [error,setError]=useState(false);useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data,error:sessionError})=>{setSession(data.session);setError(Boolean(sessionError));setLoading(false)}).catch(()=>{setError(true);setLoading(false)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);setError(false);setLoading(false)});return()=>data.subscription.unsubscribe()},[]);const signIn=async(email:string)=>{if(!supabase)return;const {error:signInError}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});if(signInError)throw signInError};const signOut=async()=>{if(supabase)await supabase.auth.signOut();setSession(null);setError(false)};return <C.Provider value={{session,loading,error,fixture:fixtureMode,signIn,signOut}}>{children}</C.Provider>};export const useAuth=()=>{const v=useContext(C);if(!v)throw new Error('AuthProvider missing');return v};
