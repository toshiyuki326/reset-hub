import type {BuiltContext} from '../../types/contextBuilderTypes';
export function ReasoningPanel({context}:{context:BuiltContext}){return <section className="ai-card"><div className="section-title"><h2>参照コンテキスト</h2><small>読み取り専用</small></div><dl className="ai-context"><div><dt>未完了タスク</dt><dd>{context.openTasks.length}</dd></div><div><dt>Goals</dt><dd>{context.goals.length}</dd></div><div><dt>KPIs</dt><dd>{context.kpis.length}</dd></div><div><dt>Documents</dt><dd>{context.documents.length}</dd></div></dl><p className="ai-note">{context.community.name} / {context.identity.displayName}（{context.identity.role}）の実データから構築しています。</p></section>}
