import type {BuiltContext,ContextBuilderInput} from '../../types/contextBuilderTypes';
export function buildContext(input:ContextBuilderInput,generatedAt:string):BuiltContext{return {version:1,generatedAt,identity:{...input.currentUser},community:{...input.community},project:input.project?{...input.project}:undefined,goals:[...input.goals].sort((a,b)=>a.id.localeCompare(b.id)),kpis:[...input.kpis].sort((a,b)=>a.id.localeCompare(b.id)),openTasks:input.tasks.filter(t=>!['done','cancelled'].includes(t.status)).sort((a,b)=>(a.dueAt||'').localeCompare(b.dueAt||'')),documents:[...input.documents].sort((a,b)=>a.id.localeCompare(b.id)),conversationHistory:input.conversationHistory.slice(-50),relevantActivity:[...input.relevantActivity].sort((a,b)=>b.createdAt.localeCompare(a.createdAt)).slice(0,50)}}
