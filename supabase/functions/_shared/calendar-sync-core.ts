export type CalendarEvent={title:string;description:string;location:string;start_at:string;end_at:string;all_day:boolean;google_calendar_event_id?:string|null};
export type CalendarApi={insert:(body:Record<string,unknown>)=>Promise<{id:string}>;patch:(id:string,body:Record<string,unknown>)=>Promise<void>;delete:(id:string)=>Promise<void>};
export function googleEventBody(event:CalendarEvent){return {summary:event.title,description:event.description,location:event.location,start:event.all_day?{date:event.start_at.slice(0,10)}:{dateTime:event.start_at,timeZone:'Asia/Tokyo'},end:event.all_day?{date:event.end_at.slice(0,10)}:{dateTime:event.end_at,timeZone:'Asia/Tokyo'}}}
export async function syncCalendarEvent(event:CalendarEvent,operation:'create'|'update'|'delete'|'upsert',api:CalendarApi){const body=googleEventBody(event);if(operation==='delete'){if(event.google_calendar_event_id)await api.delete(event.google_calendar_event_id);return null}if(event.google_calendar_event_id){await api.patch(event.google_calendar_event_id,body);return event.google_calendar_event_id}return (await api.insert(body)).id}
